# Changelog

All notable changes to the `adguard` skill are recorded here.

## 2026-06-15

### Added

- Added `scripts/adguard-api.sh` for repeatable AdGuard status, stats, query log, filtering, and host-check calls.
- Added packaging documentation so the skill has a README and changelog alongside `SKILL.md`.
