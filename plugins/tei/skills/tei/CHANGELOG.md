# Changelog

All notable changes to the `tei` skill are recorded here.

## 2026-06-15

### Added

- Added `scripts/tei-api.sh` for repeatable TEI health, info, embedding, sparse embedding, rerank, tokenize, and OpenAI-compatible calls.
- Added packaging documentation so the skill has a README and changelog alongside `SKILL.md`.
