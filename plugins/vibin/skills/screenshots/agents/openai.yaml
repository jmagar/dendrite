interface:
  display_name: "Screenshots"
  short_description: "Fetch or capture desktop screenshots from a remote Windows target over SSH."
  default_prompt: "Use $screenshots to fetch an existing screenshot, capture via ShareX, or use the PowerShell fallback."
