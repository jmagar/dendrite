interface:
  display_name: "MCP Gateway Tools"
  short_description: "Discover and call hidden upstream MCP tools through the Lab gateway search/execute surface."
  default_prompt: "Use $mcp-gateway-tools to search the live Lab gateway catalog and call the returned tool IDs through execute."
