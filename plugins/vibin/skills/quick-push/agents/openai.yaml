interface:
  display_name: "Quick Push"
  short_description: "Stage, commit, and push the current repo with the expected quick-push workflow."
  default_prompt: "Use $quick-push to save the session when needed, stage intended changes, commit with trailer, and push."
