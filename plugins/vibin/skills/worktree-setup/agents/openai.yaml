interface:
  display_name: "Worktree Setup"
  short_description: "Make a git worktree as warm and configured as main"
  default_prompt: "Use $worktree-setup to bring this worktree to parity with the main checkout: find or create the setup script, copy secrets and local config, warm the caches, trust the shell tooling, and verify it is non-degraded."
