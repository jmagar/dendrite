{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "swag-mcp",
  "description": "SWAG reverse proxy configuration management via MCP. Create, edit, view, and manage nginx proxy configurations with auth integration.",
  "author": {
    "name": "Jacob Magar"
  },
  "repository": "https://github.com/jmagar/swag-mcp",
  "license": "MIT",
  "keywords": [
    "swag",
    "nginx",
    "reverse-proxy",
    "homelab",
    "mcp"
  ],
  "userConfig": {
    "swag_proxy_confs_path": {
      "type": "string",
      "title": "SWAG proxy configs path",
      "description": "Local filesystem path to the SWAG nginx proxy-confs directory. Leave empty when using an SSH URI instead.",
      "required": false,
      "default": ""
    },
    "swag_proxy_confs_uri": {
      "type": "string",
      "title": "SWAG proxy configs SSH URI",
      "description": "Optional SSH URI for the remote SWAG proxy-confs directory, such as user@host:/mnt/appdata/swag/nginx/proxy-confs. Takes precedence over the local path when set.",
      "required": false,
      "default": ""
    },
    "swag_mcp_url": {
      "type": "string",
      "title": "Remote SWAG MCP URL",
      "description": "Optional remote Streamable HTTP MCP endpoint for swag-mcp, used by the swag-mcp-remote server entry.",
      "required": false,
      "default": ""
    },
    "swag_mcp_token": {
      "type": "string",
      "title": "Remote SWAG MCP bearer token",
      "description": "Optional bearer token for the remote swag-mcp endpoint.",
      "sensitive": true,
      "required": false
    }
  }
}
